#/usr/bin/env python
import numpy as np 

if __name__ == '__main__' :
    X = np.random.randn(500, 5)
    Y = np.array([(x[0] + 2*x[1] - 3*x[2] + x[3] - 5*x[4]) for x in X])
    print "%s %s %s %s" % (5, 10, 10, 1)
    print X[1]
    for x in range(len(X)) : 
        print X[x]
        # print "%s %s %s %s %s %s" % (X[x][0], X[x][1], X[x][2], X[x][3], X[x][4], Y[x][])
